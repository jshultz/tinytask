<%= form_for([@project, @project.tasks.build]) do |f| %>

    <div class="field">
      <%= f.label :name %><Br/>
      <%= f.text_field :name %>
    </div>
    <div class="field">
      <%= f.label :details %><br/>
      <%= f.text_area :details %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>